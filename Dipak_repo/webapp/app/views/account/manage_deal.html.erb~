<%- @page_title = display_page_title_for_change_delete_reason -%><%= "kokko"%>
<fieldset>

<%- form_for(:profile_form, :url => {:controller => "account", :action => "update_deal", :id => @deleted_profile.id}, :html => { :method => :post }) do |f| -%>

<div class="form" style="font-size: 1.2em">
  <p><b>We're Curious...why are you deleting.</b></p>
  <p><b>Deleted Date: </b><%= @deleted_profile.deal_deleted_at.to_date unless @deleted_profile.deal_deleted_at.blank? %></p>

  <p>&nbsp;</p>

  <table>

    <tr>
      <th class="label_required" colspan="2">Reason:
        <select name="profile_form[profile_delete_reason_id]" id="profile_form_profile_delete_reason_id" onchange="show_comment_box_on_change(this.value)">
          <option value="">---Select Reason---</option>
          <%@profile_delete_reasons.each do |profile_delete_reason| %>
            <option value="<%= profile_delete_reason.id %>" <% if profile_delete_reason.id == @deleted_profile.profile_delete_reason_id %>selected="selected"<% end %> ><%= profile_delete_reason.name %></option>
          <%end%>
        </select>
      <br/><p><%=error_message_on :deleted_profile, :profile_delete_reason_id%></p></th>
    </tr>
 </table>
  <table id="show_on_delete_comment" <% if @comment_flag %>style="display:none;margin-top:20px;"<% end %>>
    <tr>
      <th colspan="2">Can you tell us in a couple of words why you are deleting your profile?<br/><br/></th>
    </tr>

    <tr>
      <th colspan="2">Comments:<br/> <%=f.text_area("delete_reason",:cols=>60, :rows=>4, :value => @deleted_profile.delete_reason)%><br/><p><%=error_message_on :profile_form, :delete_reason%></p></th>
    </tr>
  </table>

</div>
<div style="margin-bottom: 20px">
   <a href="http://www.reimatcher.com/faq/investor-website/why-does-reimatcher-ask-me-why-im-deleting-a-profile/" target="_blank">Why Does REIMatcher always ask me why I’m deleting a Property or Buyer?</a> 
</div>

<p style="text-align: center">
  <%=submit_tag "Save", :class=>"submit_l"%>
  <%- if params['update'] == 'true' then %>
  <%=button_to_function "Cancel", "document.location='#{{:controller => "account", :action => "show_deals"}}'", :class=>"submit_l"%>
  <%- else -%>
  <%=button_to_function "Cancel", "document.location='#{url_for :controller=>'account', :action=>'show_deals'}'", :class=>"submit_l"%>
  <%- end -%>
</p>

<%- end -%>

</fieldset>

<script type="text/javascript">

  function show_comment_box_on_change(txt)
    {
      if ((txt=="") || (txt=="other_buyer") || (txt=="other_owner"))
        {
          document.getElementById("show_on_delete_comment").style.display="none";
        }
      else
        {
          document.getElementById("show_on_delete_comment").style.display="block";
        }
    }

</script>
